[package]
name = "app"
version = "0.1.0"
edition = "2024"

[dependencies]
rp-pico = "0.8"
embedded-hal = "0.2"
cortex-m-rt = "0.7"
panic-halt = "0.2"
cortex-m = "0.7"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
opt-level = "s"
lto = true
codegen-units = 1